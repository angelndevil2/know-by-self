export class Av {
    code: string;
    title: string;
    desc: string;

    constructor(code: string, title: string, desc: string) {
        this.code = code;
        this.title = title;
        this.desc = desc;
    }
}
